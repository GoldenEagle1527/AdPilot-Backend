# 日后产出：Cursor rules / skills

状态：accepted  
实现期已开始。禁令与步骤的**人读要点**仍在本目录；落地文件在 `.cursor/rules/*.mdc` 与 `.cursor/skills/`。rule 保持短、链回 Plan，不要把架构长文复制进 rule。

| 计划文件 | 落到 |
| --- | --- |
| [日后规则.md](日后规则.md) | `.cursor/rules/*.mdc` |
| [日后技能.md](日后技能.md) | `.cursor/skills/`（新建业务 / 接口 / 变更接口） |
