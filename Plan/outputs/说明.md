# 日后产出：Cursor rules / skills

状态：accepted  
范围：实现期要从本计划**生成**什么。现在不创建 `.cursor/rules`、不写 skill 文件、不把 Plan 正文搬进 rule。

| 计划文件 | 实现期落到 |
| --- | --- |
| [日后规则.md](日后规则.md) | `.cursor/rules/*.mdc`（短禁令，指向 Plan） |
| [日后技能.md](日后技能.md) | skills（新建业务 / 接口 / 页面 / 变更接口 / 搭前后端框架） |

生成规则：rule 只写禁令 + 链接回 Plan；禁止在 rule 里复制架构长文。
