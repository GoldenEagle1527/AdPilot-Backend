# 业务后端包

状态：accepted  
范围：一个业务包内部，好让不同人领不同接口文件。

## 必须有（实现期）

| 文件 | 职责 |
| --- | --- |
| `api/` | 按资源或按接口拆 router 文件 |
| `schemas/` | 与契约对齐的对外 DTO |
| `__init__.py` | 导出该业务 router |

## 按需

`domain/`、应用服务。跨业务只通过对方公开接口。`system_admin` 的公开窄口见该块 `后端.md` 的 B 类。

新接口：先写 `contracts/<endpoint>.md` 再写 router。`system-admin` 已有契约与实现；改接口走契约版本规则。
