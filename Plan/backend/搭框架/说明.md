# 后端搭框架

状态：accepted  
负责人：未指定（一个人领整份，不要拆成「你写配置我写 ORM」）  
对照代码：**后端应用仓**（实现期另 init，不要建在当前 `Plan/` 工作区里）

这一份是 **把后端进程搭起来**，不是实现系统管理业务。业务接口仍等契约。

并行：另一人走 [`../../frontend/搭框架/说明.md`](../../frontend/搭框架/说明.md)。接头只有信封、CORS、健康检查，见下。

## 必读

1. [`../../meta/公司研发规范.md`](../../meta/公司研发规范.md)
2. [`../FastAPI目录.md`](../FastAPI目录.md)
3. [`../数据层.md`](../数据层.md)
4. [`../../architecture/信封与分页.md`](../../architecture/信封与分页.md)
5. [`../../architecture/前后端分机.md`](../../architecture/前后端分机.md)
6. [`../../architecture/部署.md`](../../architecture/部署.md)
7. [`../../architecture/共享白名单.md`](../../architecture/共享白名单.md)

## 范围

| 做 | 不做 |
| --- | --- |
| FastAPI 进程、`core` 白名单、CORS、日志、信封、分页工具 | 投放/素材/报表 CRUD |
| 从 `deployment/*.yaml` 读配置（不用 dotenv） | 用 FastAPI 托管前端 |
| 连 192.168.112.12 共用 PG18 / Redis 7.2 | 安装宝塔；本机再起库；引入 LGPL 驱动 |
| 空挂 `system_admin`；本阶段**定义**登录/用户/权限/角色 | 写死后端 IP 进业务包；实现未写契约的 CRUD |
| `GET /api/v1/health`、`/api/v1/ready`；监听 `0.0.0.0` 并开防火墙 | 选定前端框架 |

## 可写路径（实现期）

```
# 后端应用仓
deployment/  dockerfile/  app/  …
```

不要改前端应用仓。规格以本进程 OpenAPI 为准；变更发对接文本。

## 步骤与验收

- 执行：[步骤.md](步骤.md)
- 完成定义：[验收.md](验收.md)
