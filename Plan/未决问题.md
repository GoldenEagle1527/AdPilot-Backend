# 未决问题

解决后改 `resolved` 并链 ADR，不要删编号。

## Git

| ID | 状态 | 问题 |
| --- | --- | --- |
| Q-GIT-1 | resolved | 产品名 **AdPilot**。两仓：后端 `AdPilot`、前端 `AdPilot-Frontend`。ADR 0016/0018。 |
| Q-GIT-2 | resolved | 大文件放云盘，小文件进 git。ADR 0016。 |
| Q-GIT-3 | resolved | 本工作区已本地 `git init`（只是写计划）；应用仓实现期再 init。不加 origin。ADR 0016/0019。 |
| Q-GIT-4 | resolved | 两个应用仓。不建计划仓。对接：OpenAPI + 群里对接文本。ADR 0018/0019。 |

## 认证（shared 槽位，不是业务块）

| ID | 状态 | 问题 |
| --- | --- | --- |
| Q-AUTH-1 | resolved | Token 放 **Authorization Header**。ADR 0016。 |
| Q-AUTH-2 | resolved | **密码登录**；当前 **mock 数据**。ADR 0016。 |

## 前端与业务

| ID | 状态 | 问题 |
| --- | --- | --- |
| Q-FE-1 | resolved | **React**。ADR 0015。 |
| Q-FE-2 | resolved | 投放/素材/报表块先不创建。ADR 0016。 |
| Q-MOD-1 | resolved | 本批锁定 `system-admin`。ADR 0008。 |

## 部署

| ID | 状态 | 问题 |
| --- | --- | --- |
| Q-DEP-1 | resolved | 开发：后端本机监听并开防火墙；前端另一台机器，URL 发群后写入 CORS。Docker + yaml + 共用库见 ADR 0017。 |
| Q-CI-1 | resolved | 暂无 CI/CD，手动发布。ADR 0016。 |

## 数据

| ID | 状态 | 问题 |
| --- | --- | --- |
| Q-BE-1 | resolved | PostgreSQL 18.x + SQLAlchemy 2 asyncio + asyncpg；Redis 7.2.x。ADR 0014。 |
| Q-BE-2 | resolved | 前端拉后端 **OpenAPI**。变更另发对接短文。ADR 0019。 |

## 权限形状

| ID | 状态 | 问题 |
| --- | --- | --- |
| Q-PERM-1 | resolved | 机制：角色↔树 + 用户↔可见部门。ADR 0008/0010。 |
| Q-PERM-6 | resolved | 清单按 `5.4.0.权限管理-权限角色查询表.xlsx`。ADR 0016。 |
| Q-PERM-2 | resolved | 有效功能权限 = 用户角色 ∪ 部门角色；负责人/成员不改并集。ADR 0016。 |
| Q-PERM-3 | resolved | 勾父 = 全选子孙（可再取消个别）；空集合 = 无数据权限。ADR 0016。 |
| Q-PERM-4 | resolved | 用户可多挂角色，节点去重；数据权限冲突取并集。ADR 0016。 |
| Q-PERM-5 | resolved | 默认密码在库中字典配置，不写死 135。ADR 0016。 |
| Q-TENANT-1 | resolved | 用户/部门有租户字段。ADR 0016。 |

## 仍开口（执行时补，不是产品未决）

| 项 | 说明 |
| --- | --- |
| 前端开发 origin | 对方发群后写入 `deployment/dev.yaml` 的 `cors_origins` |
| 后端局域网地址 | 后端机开端口后告知前端，写入 `api_base` |
| 框架负责人姓名 | 回复未填 |
