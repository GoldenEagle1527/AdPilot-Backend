# Drooling 计划总入口

本目录在**后端应用仓** `AdPilot-Backend/Plan/`。只规划本仓 FastAPI。

**后端框架已落地**（真源是 `backend/app/`、`deployment/`、`docker-compose.yml`）。开发/测试：Postgres 18、Redis 7.2、API 一律本机 Docker（ADR 0020）。`192.168.112.12` 只给生产。

第一块业务是 **系统管理（`system-admin`）**。授权机制在该包；清单按 xlsx（ADR 0016），不要为树节点预建投放 CRUD。本进程 **只提供 API**：CORS、OpenAPI、对接短文（ADR 0012/0019）。栈见 ADR 0014。公司规范见 [`meta/公司研发规范.md`](meta/公司研发规范.md)。

Agent 规范在 `.cursor/rules/` 与 `.cursor/skills/`。

## 先读

1. [`一览.md`](一览.md) — 模块 / 认领 / 通信 / 权限机制
2. [`backend/接口变更对接.md`](backend/接口变更对接.md) — 改接口必发的短文
3. [`backend/FastAPI目录.md`](backend/FastAPI目录.md) — 对照 `backend/app/`
4. [`architecture/融合.md`](architecture/融合.md) — 切块怎么套到系统管理
5. [`meta/已定.md`](meta/已定.md) — 已定树
6. [`meta/Git与Gitea.md`](meta/Git与Gitea.md) — `feat/*` + PR

新业务复制 [`modules/_template/`](modules/_template/)，不要自建第二套权限台。不要在本仓写页面或托管静态前端。

## 目录

```
Plan/
  architecture/             切块、shared、部署、信封
  modules/system-admin/     第一块：模型 + 契约 + 后端认领
  modules/_template/        以后的业务
  backend/                  目录约定 + 数据层 + 对接文本
  contracts/                契约写法
  outputs/                  禁令/技能要点
  decisions/                ADR
```

## 当前阶段

system-admin 契约见 [`modules/system-admin/contracts/最新表.md`](modules/system-admin/contracts/最新表.md)；实现已在 `app/modules/system_admin`。新工作：按业务认领 **接口文件**；`feat/<business>/<short>` + PR 合 `main`。
