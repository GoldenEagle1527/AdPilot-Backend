# Drooling 计划总入口

本目录在**后端应用仓** `AdPilot-Backend/Plan/`。`H:\Projects\drooling\Plan` 已废弃。

开发/测试：Postgres 18、Redis 7.2、后端进程一律本机 Docker（ADR 0020）。`192.168.112.12` 只给生产。

模块化约定已经和第一块业务 **系统管理（`system-admin`）** 对齐。

一个人可以只领一个接口或一个页面。授权**机制**在 `system-admin`；清单按 xlsx（ADR 0016），投放页不预建。前后端 **两台机器、两个应用 Git 仓**。对接由后端做主：OpenAPI + 群里对接短文（ADR 0019）。`Plan/` 只是现在写计划。前端 React（ADR 0015）。后端已定版（ADR 0014）。公司规范见 [`meta/公司研发规范.md`](meta/公司研发规范.md)。

「产出 Cursor rule」仍是日后产出，见 [`outputs/`](outputs/说明.md)。现在不创建 `.cursor/rules`。

## 先读

1. [`一览.md`](一览.md) — 模块 / 前后端 / 认领 / 通信 / 权限机制（总表）
2. [`architecture/两仓通信.md`](architecture/两仓通信.md) — OpenAPI + 对接文本
3. 搭框架：[backend/搭框架/说明.md](backend/搭框架/说明.md) · [frontend/搭框架/说明.md](frontend/搭框架/说明.md)
4. [`architecture/融合.md`](architecture/融合.md) — 切块怎么套到系统管理
5. [`meta/已定.md`](meta/已定.md) — 已定树

新业务仍复制 [`modules/_template/`](modules/_template/)，不要自建第二套权限台。

## 目录

```
Plan/
  architecture/融合.md   模块化 × 系统管理（从这里进）
  architecture/             切块通则、shared、技术栈
  modules/system-admin/     第一块业务：模型 + 认领面
  modules/_template/        以后的业务
  backend/搭框架/           一人：把后端进程搭起来
  frontend/搭框架/          一人：把前端应用搭起来（React）
  backend/ frontend/        目录约定 + 数据层
  contracts/                契约写法（最新表 + 历史）；system-admin 字段表尚未写
  outputs/                  日后 rules / skills
  decisions/                ADR
```

## 当前阶段

**阶段 2c 已起草并核对：** system-admin 契约见 [`modules/system-admin/contracts/最新表.md`](modules/system-admin/contracts/最新表.md)。前端可按此并行；真源落地后仍是后端 OpenAPI。

**阶段 2d：两人搭框架。** 本阶段定义登录/用户/权限/角色。投放 CRUD 不做。  
允许：按 `搭框架/` 起进程、健康检查、Header Token、登录 mock；契约已写的 system-admin 接口可实现。  
禁止：预建 102 个业务页、复刻截图、宝塔、违规许可证、把规范写进 `.cursor/rules`。
