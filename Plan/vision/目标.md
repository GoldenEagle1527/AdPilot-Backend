# 目标

状态：accepted

1. **按业务切块**，便于多人维护。第一块是 `system-admin`。
2. **块内再拆**到单个接口 / router 文件。
3. **领接口的人先写契约**。
4. **授权机器在 `system-admin`**；其它业务用公开窄口，不自建权限台。
5. **本仓只提供 API**：CORS、OpenAPI、对接短文。
6. **栈 + 公司规范**：PG18、Redis 7.2、宽松许可证、禁宝塔。

非目标：复刻截图；预建投放/素材/报表业务块；在本仓写前端。
