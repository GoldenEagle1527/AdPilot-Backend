# 契约：create-department-tag

业务id：system-admin
文档版本：2
方法：POST
路径：/api/v1/system-admin/department-tags
作用：新增部门标签名。

作者：调度者
状态：accepted
更新日期：2026-09-20

## 请求

| 字段 | 位置（path/query/body/header） | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
| name | body | string | 是 | 仅允许：投放部、投放组、素材部、素材组 |

## 响应

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | |
| name | string | |

`data` 即 `Tag`。

## 错误

| 情况 | HTTP 状态 | 说明 |
| --- | --- | --- |
| 同名 | 409 | `CONFLICT` |
| 不在枚举内 | 422 | `TAG_NOT_ALLOWED` |

其余见 [说明.md](说明.md) 共用错误码。

## 被谁调用

| 调用方 | 动作 |
| --- | --- |
| 部门管理 | 新增部门标签 |

## 变更历史

新记录插在最上行。破坏性变更须先把本文快照到 `_history/<endpoint>-v<旧版本>.md`。

| 日期 | 文档版本 | 破坏？ | 变更 | 作者 |
| --- | --- | --- | --- | --- |
| 2026-09-20 | 2 | 否 | 名称锁定四项枚举 | 调度者 |
| 2026-09-20 | 1 | 否 | 初稿 | 调度者 |
