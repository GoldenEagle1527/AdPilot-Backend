# 契约：set-role-menus

业务id：system-admin
文档版本：2
方法：PUT
路径：/api/v1/system-admin/roles/{id}/menus
作用：整集替换角色已勾选的菜单节点；服务端不自动展开子孙。

作者：调度者
状态：accepted
更新日期：2026-09-21

## 请求

| 字段 | 位置（path/query/body/header） | 类型 | 必填 | 说明 |
| --- | --- | --- | --- | --- |
| id | path | string | 是 | 角色 id |
| menu_ids | body | string[] | 是 | 整集替换。请求体提交什么就存什么 |

勾父目录是否等于勾全部子孙【未证实】——本接口按提交的 id 集合落库，**不**在服务端自动展开子孙。

## 响应

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| menu_ids | string[] | 写入后的集合 |

## 错误

| 情况 | HTTP 状态 | 说明 |
| --- | --- | --- |
| 节点不存在 | 404 | `menu_ids` 中任一 id 不是已有 MenuNode。code：`NOT_FOUND` |
| 完成后无人再持有用户管理菜单 | 409 | `LAST_ADMIN_REQUIRED` |

角色不存在同样 404 `NOT_FOUND`。其余共用错误码见 [说明.md](说明.md)。

## 被谁调用

| 调用方 | 动作 |
| --- | --- |
| 角色管理 | 保存勾选 |

## 变更历史

新记录插在最上行。破坏性变更须先把本文快照到 `_history/<endpoint>-v<旧版本>.md`。

| 日期 | 文档版本 | 破坏？ | 变更 | 作者 |
| --- | --- | --- | --- | --- |
| 2026-09-21 | 2 | 否 | 去掉用户管理菜单后须仍有用户管理员 | 姜英睿 |
| 2026-09-20 | 1 | 否 | 初稿 | 调度者 |
