# 页面：`<page>`

模块：`<id>`  
代码落点（日后）：`frontend/src/modules/<id>/pages/<page>/`  
领谁：

## 干什么

一句话。不要写布局。

## 调用

| 页面动作 | 契约 | 用到的字段名（类型见契约） |
| --- | --- | --- |
| | [../contracts/接口契约.模板.md](../contracts/接口契约.模板.md) | 契约未写则本行不可实现 |

没有契约文件的调用不准开工。不要在本文件复制请求/响应表。默认对着契约当前正文。必须停在旧版时写文档版本并链 `../contracts/_history/`。

## 明确不做

-
