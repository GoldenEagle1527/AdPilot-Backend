# 后端：`file`

状态：draft

列出接口（认领）。每一行必须指向一份契约。当前版本与路径以 [contracts/最新表.md](contracts/最新表.md) 为准。

| 契约文件 | 方法与路径 | 领谁 | 状态 |
| --- | --- | --- | --- |
| [contracts/upload-file.md](contracts/upload-file.md) | POST /api/v1/files/upload | | draft |

## 依赖的其他业务

无。登录态用 `core` 的 `require_token`。对象存储用 `app.storage.tos`。

## 明确不做

- 多文件一次上传
- 落库、转码、封面与第一帧抽取
- 菜单节点鉴权（只认登录态）
