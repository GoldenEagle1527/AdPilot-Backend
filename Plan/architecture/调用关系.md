# 调用记录

状态：accepted  
本仓只维护契约与实现。参数真源是 `contracts/<endpoint>.md`；对外真源是 OpenAPI。

| 问题 | 写在哪 |
| --- | --- |
| 提供哪些接口、参数、干什么 | `modules/<id>/contracts/<endpoint>.md`；`最新表.md`；`后端.md` |
| 接口改过什么 | 契约底部变更历史；破坏性快照 `_history/` |
| 谁在用 | 契约里「被谁调用」一句话即可，不要另做页面卡片 |

改接口时 Agent 必做：[契约版本.md](契约版本.md)。实现与最新表不一致 = 未完成。
