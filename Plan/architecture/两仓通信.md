# 对接：OpenAPI + 短文

状态：accepted  
ADR：[0019](../decisions/0019-后端OpenAPI对接.md)

本仓 `Plan/` 不是给调用方 clone 的接头。调用方拉 **OpenAPI**。

## 谁说了算

后端做主。需求在线下或群里定。

| 传什么 | 走哪 |
| --- | --- |
| 要做什么 | 群 / 线下 |
| 字段、类型、路径、错误码 | `/openapi.json`、`/docs` |
| 接口刚变了 / 新加了 | 群里一段对接文本（每次必发） |
| 调用方 origin | 群 → 写入本仓 `cors_origins` |

## 改接口时

1. 改代码，保证 OpenAPI 已是新的。
2. 按 [../backend/接口变更对接.md](../backend/接口变更对接.md) 发群。不贴整份 schema。

没有对接文本 = 调用方可以当没这回事。
