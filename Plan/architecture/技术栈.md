# 技术栈

状态：accepted

## 已锁定

| 层 | 选择 |
| --- | --- |
| 后端语言 | Python 3.12.x |
| Web | FastAPI + Uvicorn |
| 前端 | React（Vite + React，ADR 0015） |
| ORM / 迁移 | SQLAlchemy 2.x asyncio + Alembic |
| PG 驱动 | asyncpg（禁止 psycopg） |
| 数据库 | PostgreSQL 18.x（开发连 192.168.112.12 共用实例） |
| 缓存 | Redis 7.2.x（同上主机） |
| 配置 | yaml，目录 `deployment/`，不用 dotenv |
| 应用打包 | Docker，Dockerfile 在 `dockerfile/`；不把库打进镜像 |
| 运行 | 后端模块化单体（一业务一包，一个 FastAPI 进程） |
| 部署拓扑 | 前端机 + 后端机（ADR 0012）；开发后端本机、前端对方机器 |
| 登录 | 密码 + Header Token；当前 mock（ADR 0016） |
| 发布 | 手动，暂无 CI/CD |
| 接口描述 | 后端 OpenAPI；变更发对接短文（ADR 0019） |
| 信封 / 分页 | [信封与分页.md](信封与分页.md) |
| 合规 | [公司研发规范.md](../meta/公司研发规范.md) |
| 版本 | Git；两应用仓；远程预定 git.73oc.local，暂无 origin |

## 实现时再填

前端 origin、后端局域网 IP（发群 / 写入 `deployment/dev.yaml`）。
