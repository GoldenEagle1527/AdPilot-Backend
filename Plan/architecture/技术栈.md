# 技术栈

状态：accepted

## 已锁定

| 层 | 选择 |
| --- | --- |
| 语言 | Python 3.12.x |
| Web | FastAPI + Uvicorn |
| ORM / 迁移 | SQLAlchemy 2.x asyncio + Alembic |
| PG 驱动 | asyncpg（禁止 psycopg） |
| 数据库 | PostgreSQL 18.x（开发 Compose；生产 192.168.112.12） |
| 缓存 | Redis 7.2.x（同上） |
| 配置 | yaml，`deployment/`，不用 dotenv |
| 应用打包 | `dockerfile/backend.Dockerfile`；库不打进镜像 |
| 运行 | 一业务一包，一个 FastAPI 进程 |
| 部署 | 本仓只提供 API（ADR 0012）；开发本机 Compose |
| 登录 | 密码 + Header JWT（HS256）；查库，会话按 jti 存 Redis（ADR 0016） |
| 发布 | 手动，暂无 CI/CD |
| 接口描述 | OpenAPI；变更发对接短文（ADR 0019） |
| 信封 / 分页 | [信封与分页.md](信封与分页.md) |
| 合规 | [公司研发规范.md](../meta/公司研发规范.md) |
| 版本 | Git；origin 已指向 AdPilot-Backend；合入走 PR |

## 执行时再填

调用方 origin 发群后写入 `deployment/dev.yaml` 的 `cors_origins`。
