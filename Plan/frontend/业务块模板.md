# 前端业务块（计划写什么）

状态：accepted

`modules/<id>/前端.md` 做页面目录。每个页面一份 `pages/<page>.md`（调用表）。规则：[调用关系.md](../architecture/调用关系.md)。页面请求打到后端机，见 [前后端分机.md](../architecture/前后端分机.md)。

不要复制请求/响应字段——字段以契约**当前正文**（最新表）为准。旧版只链 `_history/`。
