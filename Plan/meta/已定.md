# 已定树

状态：accepted  
改其中任何一条必须新开 ADR。

## 现在在做什么

后端框架已落地。做 **system-admin 与后续业务接口**。投放/素材/报表不建块。不要复刻截图视觉。

## 模块怎么切

```
system-admin（已有）          未来：ad-delivery / material / report …（先不建）
  ├─ 包内按资源拆 router
  └─ 授权台 + 公开能力 B
core：登录态（Header）、信封、CORS、DB/Redis
```

- 一个 FastAPI 进程，一个业务一个包。
- Python 3.12、FastAPI、PG18、SQLAlchemy、asyncpg、Redis 7.2。
- 本仓只提供 API；开发本机 Compose，生产才用 192.168.112.12。
- Git：本仓已加 origin。对接：OpenAPI + 群里对接短文。
- 权限机制 + xlsx 清单。

细则：[architecture/融合.md](../architecture/融合.md)

## 并行

按接口文件认领。未开块的业务等契约再接。
