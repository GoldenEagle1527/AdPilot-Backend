---
description: 模块开发用 feat 分支，合入必须走 PR。任何改代码、开模块、提接口时必守。
alwaysApply: true
---

# 分支与 PR

保护 `main`。不要在 `main` 上开发或直推。从最新 `main` 开分支：

`feat/<business>/<short>`

例：`feat/system-admin/list-users`、`feat/ad-delivery/create-task`。  
一个分支只做一个业务。不要混前端文件。不要再用 `feat/framework`。

合入：对 `origin` 开 Pull Request → `main`（Gitea `git.73oc.local`，仓名 `AdPilot-Backend`）。不要多人直推 `main`。

一人领多个接口时，仍宜一接口一 PR。改对外 HTTP 的同一 PR 必须改契约、变更历史、`Plan/modules/<id>/contracts/最新表.md`；破坏性带 `_history/`。合入后发群对接短文。

生产密钥不准进仓。开发口令只写本机 `deployment/dev.yaml`（已 gitignore；从 `deployment/dev.yaml.example` 复制）。

人读细则：`Plan/meta/Git与Gitea.md`。
