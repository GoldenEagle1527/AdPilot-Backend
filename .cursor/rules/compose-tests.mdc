---
description: 单测必须进 Compose 的 test 服务，禁止本机直连映射端口。
alwaysApply: true
---

# 测试走 Compose

需要连 PostgreSQL / Redis 的测试（含 TestClient、`/ready`、Alembic）必须进本机 Compose 网，不要在 Windows/macOS 宿主上 `python -m unittest`。宿主解析不到 `postgres`/`redis`；即便改连 `127.0.0.1`，asyncpg 走端口映射也常被掐断。

仓库根：

```
python run_tests.py
python run_tests.py tests.modules.<pkg>.test_xxx -v
```

等价：`docker compose --profile test run --rm test`。该服务会先 `alembic upgrade head`。不要为此把 `dev.yaml` 的主机改成 `127.0.0.1`（会弄坏 API 容器）。纯内存/不连库的单测可以本机跑，但新模块默认仍走 `run_tests.py`，免得漏迁表。
